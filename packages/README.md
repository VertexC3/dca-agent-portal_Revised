# npm workspaces

Shared packages for this monorepo live here (e.g. `packages/shared`).

The main Vite + React app is at the repository root. Add new workspace packages under `packages/*` and reference them from the root `package.json` workspaces field.
